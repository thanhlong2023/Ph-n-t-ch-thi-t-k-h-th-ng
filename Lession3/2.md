Chào bạn, đây là phần trình bày bằng Markdown (MD) có kèm biểu đồ Mermaid, giải thích cho yêu cầu của bạn.

---

## 💎 Sơ đồ Use Case: Ứng dụng xem phim

Dựa trên tình huống: "chức năng 'Xem chi tiết phim' **có thể mở rộng** thành 'Xem trailer' hoặc 'Xem bình luận'", mối quan hệ đúng phải là `<<extend>>` (mở rộng).

Mối quan hệ `<<extend>>` được sử dụng khi một chức năng là **tùy chọn** (không bắt buộc) và chỉ xảy ra trong một số điều kiện nhất định. Ngược lại, `<<include>>` (bao gồm) được dùng khi một chức năng là **bắt buộc**.

Trong trường hợp này, việc "Xem trailer" là một tùy chọn sau khi đã vào "Xem chi tiết phim", do đó chúng ta dùng `<<extend>>`.

### 1. Biểu đồ Use Case (Vẽ bằng Mermaid)

```graph LR
graph LR
User[Người dùng]

    subgraph "Hệ thống App"
        UC_Base("Xem chi tiết phim")
        UC_Ext1("Xem trailer")
        UC_Ext2("Xem bình luận")
    end

    User --> UC_Base
    UC_Ext1 -.->|"<<extend>>"| UC_Base
    UC_Ext2 -.->|"<<extend>>"| UC_Base
```
