# 🎬 Use Case – Ứng dụng xem phim (Phiên bản tương thích)

## 🧍‍♂️ Actor

- **Người dùng**: Người xem sử dụng ứng dụng để tìm, xem thông tin và nội dung phim.

## 📊 Sơ đồ Use Case (chuẩn UML)

```mermaid
graph TD
    %% Định nghĩa Actor bằng cú pháp node chuẩn (tương thích 100%)
    U((Người dùng))

    %% Định nghĩa các Use Case
    UC1((Xem chi tiết phim))
    UC2((Xem trailer))
    UC3((Xem bình luận))

    %% Mối quan hệ
    U --> UC1

    %% Mối quan hệ Extend (Mở rộng)
    UC2 .-> |extend| UC1
    UC3 .-> |extend| UC1
```

@startuml
left to right direction
skinparam packageStyle rectangle
actor customer
actor clerk
rectangle checkout {
customer -- (checkout)
(checkout) .> (payment) : include
(help) .> (checkout) : extends
(checkout) -- clerk
}
@enduml
