# 🎬 Use Case – Ứng dụng xem phim (Phiên bản đã chỉnh sửa)

## 🧍‍♂️ Actor

- **Người dùng**: Người xem sử dụng ứng dụng để tìm, xem thông tin và nội dung phim.

## 📊 Sơ đồ Use Case (chuẩn UML)

```mermaid
graph TD
    %% Định nghĩa các Actor (hình người với tên ở dưới)
    actor Người_dùng

    %% Định nghĩa các Use Case
    UC1((Xem chi tiết phim))
    UC2((Xem trailer))
    UC3((Xem bình luận))

    %% Mối quan hệ
    Người_dùng --> UC1

    %% Mối quan hệ Extend (Mở rộng)
    UC2 .-> |extend| UC1
    UC3 .-> |extend| UC1
```
